

# Codec is defined as an enum also
def update(video_file, object_trackings, color=None, codec=None, outputpath=".")

# Check if video has same number of frames as object trackings
# Check if output path exists
